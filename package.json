{
  "name": "3dea",
  "version": "2.6.0",
  "private": true,
  "type": "module",
  "description": "3DEA — bibliotheque de modeles 3D avec base de donnees Prisma",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "setup": "prisma generate && prisma migrate dev --name init && npm run db:seed",
    "db:seed": "node prisma/seed.js",
    "db:studio": "prisma studio",
    "db:push": "prisma db push",
    "use:sqlite": "node scripts/use-db.js sqlite",
    "use:postgres": "node scripts/use-db.js postgresql",
    "build:netlify": "prisma generate && prisma migrate deploy",
    "check": "node --check src/server.js && node --check src/api.js && node --check src/storage.js && node --check prisma/seed.js && node --check netlify/functions/api.js"
  },
  "prisma": { "seed": "node prisma/seed.js" },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "@prisma/client": "^5.22.0",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
